Drop reaction GIF, WebP, PNG, or JPEG files here with any filenames, then run:

  npm run reactions:index

The generated manifest.json is used by the app. Keep images reasonably small
because Vite copies this directory directly into the deployed site.
